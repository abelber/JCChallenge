using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmployeeFactory : MonoBehaviour
{
    [SerializeField]
    private Dictionary<string, IEmployee> employees;

    void Start()
    {
                
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
